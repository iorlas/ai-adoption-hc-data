[project]
name = "donor-registry-workshop"
version = "0.1.0"
description = "Claude Code workshop, Day 1 — exploring synthetic donor-registry data."
requires-python = ">=3.11"
dependencies = [
    "pandas>=2",
    "jupyterlab>=4",
    "ipykernel>=6",
]

[tool.uv]
# Not a Python package, just a managed environment for the exercises.
package = false
